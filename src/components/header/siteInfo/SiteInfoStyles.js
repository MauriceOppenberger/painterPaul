import styled from "styled-components"

export const SiteInfoWrapper = styled.div`
  h1 {
    margin: 0 auto;
    color: var(--mainDark);
  }

  a {
    color: var(--mainDark);
    text-decoration: none;
  }
`
